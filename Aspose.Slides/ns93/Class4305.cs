namespace ns93;

internal abstract class Class4305
{
	private Enum511 enum511_0;

	public Enum511 Type => enum511_0;

	protected Class4305(Enum511 type)
	{
		enum511_0 = type;
	}

	public abstract override string ToString();
}
